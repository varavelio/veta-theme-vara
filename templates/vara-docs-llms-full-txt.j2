{% set docs_routes = vara_docs_routes() %}
{% set docs_raw_pages = vara_docs_raw_pages(docs_routes.root) -%}
## Documentation index

This index lists every available documentation page and its Markdown source.

{% for item in docs_raw_pages %}
  {{- item.indent }}- [{{ item.title|safe }}]({{ vara_absolute_url(item.permalink) }})
{% endfor %}

## Documentation content

The complete documentation for this website follows, reproduced verbatim from every page.

{% for item in docs_raw_pages %}---

{{ item.content|safe }}

{% endfor %}
