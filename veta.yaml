# Veta configuration file.
# Read more: https://veta.varavel.com/config

build:
  output: dist
  clean: true

dev:
  watch:
    - content

html:
  minify: true

tailwindcss:
  stylesheets:
    - styles.css
  minify: true
